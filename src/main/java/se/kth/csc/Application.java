package se.kth.csc;

/**
 * Marker interface that marks the root of our web application. This allows us to do
 * {@code Application.class.getPackage().getName()} instead of hard-coding the base package.
 */
public interface Application {
}
